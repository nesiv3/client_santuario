import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elsantuario.app',
  appName: 'el-santuario',
  webDir: 'dist/angular-client/browser'
};

export default config;
